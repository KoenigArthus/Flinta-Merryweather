using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testjaja : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
      Debug.Log("ja")  
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
